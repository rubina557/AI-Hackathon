# Agent 3: Multi-Criteria Matrix Ranking
- Sort candidates mathematically using the system weight function:
  Score = (Rating/5.0 * 35) + (Skill_Level_Weight * 25) + Max(0, 100 - (distance_km * 6.5)) + (Badges * 10)
- Verify that each provider profile has open availability blocks intersecting with the user's requested time_frame.
